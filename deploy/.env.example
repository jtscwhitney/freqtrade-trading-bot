# Freqtrade / Log API credentials (matches config_sniper_BTC_DryRun.json api_server)
# Copy to .env and fill in. Never commit .env to git.
FREQTRADE_API_USERNAME=freqtrader
FREQTRADE_API_PASSWORD=SuperSecurePassword123

# Exchange API keys - add these to user_data/config_sniper_BTC_DryRun.json
# or use environment variable injection if your config supports it
# BINANCE_API_KEY=
# BINANCE_API_SECRET=
